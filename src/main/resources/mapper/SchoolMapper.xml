<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jckj.mapper.SchoolMapper">
    <select id="list" parameterType="com.jckj.model.School" resultType="com.jckj.model.School">
        select id,school_name schoolName,school_address schoolAddress,school_introduce schoolIntroduce,school_phone schoolPhone,school_photo schoolPhoto,is_delete isDelete,create_time createTime,update_time updateTime from t_school_info
        where is_delete=0
        limit #{dataNum},#{limit}
    </select>
    <select id="count" parameterType="com.jckj.model.School" resultType="java.lang.Integer">
        select
        count(1)
        from t_school_info
        where is_delete = 0
    </select>
</mapper>